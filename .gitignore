# Frontend
frontend/node_modules/
frontend/dist/

# Env files
.env
.env.local

# Local per-host compose overrides (e.g. running a second stack on a host
# that already has one, with renamed containers to avoid name collisions).
docker-compose.override.yml

# repair.sh runtime artifacts
repair.log
backup-*.sql

# OS / editor
.DS_Store
.vscode/
.idea/

# Generated report PDFs (mounted into the backend container).
/reports/
